# ubuntu 18.04 LTS
```
sudo apt-get install libboost-all-dev libfreetype6-dev \
 libgdal-dev zlib1g-dev clang make pkg-config curl libmapnik-dev \
  libmapnik3.0 mapnik-utils python-mapnik
pip install -r requirements.txt

```